server:
  servlet:
    context-path: /bookshelf

spring:
  mvc:
    servlet:
      path: /api/v1
  data:
    mongodb:
      authentication-database: admin
      username: test
      password: test
      database: bookshelf-test
      port: 27017
      host: localhost